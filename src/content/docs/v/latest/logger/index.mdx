---
title: Logger
description: Structured logging — channels, redaction, lifecycle hooks, graceful shutdown.
---

import HighlightFeatures from "../../../../../components/HighlightFeatures.astro";

> Standalone — usable in any Node project, no `@warlock.js/core` required.

`@warlock.js/logger` is the logging primitive. Structured by default,
channel-based (route different levels or modules to different sinks),
with PII redaction built in, lifecycle hooks for app boot/shutdown, and
buffered flushes so you never lose entries on a hard exit.

<HighlightFeatures
  kicker="why logger"
  title="Production logging without ceremony"
  subtitle="Five primitives that make logs you can actually trust in prod — not console noise."
  features={[
    {
      icon: "M4 6h16M4 12h16M4 18h10",
      title: "Channels",
      lede: "One config, many sinks.",
      body: "Register channels once; each one filters by level or a custom predicate. info to the console, JSON to a file, errors wherever you route them.",
      code: "log.configure({ channels: [new ConsoleLog(), new FileLog({ levels: [\"error\"] })] })",
    },
    {
      icon: "M12 2 4 6v6c0 5 3.5 9 8 10 4.5-1 8-5 8-10V6Z",
      title: "PII redaction",
      lede: "Sensitive fields, automatically masked.",
      body: "Configure the paths once; values at those paths are scrubbed before they ever reach a sink.",
      code: "log.configure({ redact: { paths: [\"context.token\"] } })\nlog.info(\"auth\", \"signup\", \"ok\", { email, token }) // token redacted",
    },
    {
      icon: "M3 12a9 9 0 1 0 18 0 9 9 0 0 0-18 0Zm9-5v5l3 2",
      title: "Graceful shutdown",
      lede: "No lost lines on exit.",
      body: "Buffered writes flush on SIGTERM/SIGINT/beforeExit so the last entries survive a clean shutdown.",
      code: "log.configure({ autoFlushOn: [\"SIGTERM\", \"SIGINT\", \"beforeExit\"] })",
    },
    {
      icon: "M12 9v3m0 4h.01M5.07 19h13.86a2 2 0 0 0 1.74-3l-6.93-12a2 2 0 0 0-3.48 0L3.33 16a2 2 0 0 0 1.74 3Z",
      title: "Uncaught capture",
      lede: "Errors you didn't know to catch.",
      body: "One call wires unhandledRejection + uncaughtException into your channels with the full stack and request context.",
      code: "captureAnyUnhandledRejection()",
    },
  ]}
/>

---

Ready to wire it in? **[Installation →](./getting-started/01-installation/)** — three lines, five minutes.
